# Server Configuration
PORT=8000
NODE_ENV=production # Set to 'development' for local testing

# Database Configuration
DATABASE_NAME=youtube-backend-api
# For Atlas (production): include database name in URI
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/youtube-backend-api
# For local development: use basic URI
# MONGODB_URI=mongodb://localhost:27017

# CORS Configuration
CORS_ORIGIN=https://yourdomain.com
# CORS_ORIGIN=* # Use this for local development, but restrict in production

# JWT Configuration
ACCESS_TOKEN_SECRET=your-super-secure-access-token-secret-256-bits-minimum
ACCESS_TOKEN_EXPIRY=1d
REFRESH_TOKEN_SECRET=your-super-secure-refresh-token-secret-256-bits-minimum
REFRESH_TOKEN_EXPIRY=10d

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with actual production values
# 3. Use strong, randomly generated secrets for JWT tokens
# 4. Set CORS_ORIGIN to your frontend domain
# 5. Use production MongoDB Atlas connection string
# 6. Never commit .env file to version control
